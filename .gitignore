# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Node
node_modules/
frontend/dist/

# Credentials (NEVER commit)
# 実体は .secrets/ に隔離してある（ISS-426）。名前による除外は改名で破れるので、
# ディレクトリごと除外する側を主とする。以下の3行は保険。
.secrets/
token.json
gmail_token.json
credentials.json
*.pickle

# IDE
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.local

# 写真バックアップ（ローカルのみ・再圧縮前の原本）
frontend/public/photos_backup_*/

# ログ・作業ファイル
*.log
.astro/

# Claude Code のプレビュー設定（開発者ローカル・ISS-585）
.claude/
